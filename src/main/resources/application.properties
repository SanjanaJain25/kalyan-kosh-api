spring.application.name=kalyan-kosh-api

spring.datasource.url=jdbc:mysql://localhost:3306/kalyankosh_db?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
# spring.datasource.password=download

spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2

spring.jpa.hibernate.ddl-auto=update
# Set to 'update' to let Hibernate create new State and Sambhag tables
# After everything is working, can change to 'validate' for production
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

app.jwt.secret=${JWT_SECRET:change_this_to_a_very_long_secret_key_please_change}
app.jwt.expiration-ms=${JWT_EXP_MS:43200000}
app.jwt.mobile-expiration-ms=${MOBILE_JWT_EXP_MS:315360000000}
# JWT expiration: 43200000 ms = 12 hours (comment moved to new line)

server.port=8080
logging.level.org.springframework.security=DEBUG
logging.level.com.example.kalyan_kosh_api.config=DEBUG

# ============================================
# Email Configuration - Amazon SES SMTP
# ============================================

app.email.enabled=true

# Sender email must be verified in SES.
# Since pmums.com is verified, you can use support@pmums.com or noreply@pmums.com
app.email.from=support@pmums.com

spring.mail.host=email-smtp.ap-south-1.amazonaws.com
spring.mail.port=587

# Use Amazon SES SMTP username/password here, not AWS access key/secret key
spring.mail.username=AKIAWWDH2AQTYNJBBYVQ
spring.mail.password=BE1lhlR7Kwg0O/NooL9NfDcFS5cKQqm699L/hqU/zn+g

spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=email-smtp.ap-south-1.amazonaws.com

spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true


# For production, use environment variables:
# app.email.enabled=true
# spring.mail.username=${EMAIL_USERNAME}
# spring.mail.password=${EMAIL_PASSWORD}
authkey.api-key=13ddd865712c6b2b
# authkey.api-key=DUMMY_KEY
# authkey.sender=DEVAPP

#spring.profiles.active=dev

# File Upload Configuration
# # Maximum file size for single file upload (10MB)
# spring.servlet.multipart.max-file-size=10MB
# # Maximum request size (total for all files + data in one request)
# spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB
server.tomcat.max-http-form-post-size=20MB
server.tomcat.max-swallow-size=20MB
# ============================================
# File Storage Configuration
# ============================================
# Storage type: 'local' or 's3'
# Set to 's3' to use AWS S3, 'local' for local file system
# For local development, use 'local'. For production (EC2), use 's3'
storage.type=s3
# storage.type=local
# AWS S3 Configuration (required when storage.type=s3)
# Set these values via environment variables for security
aws.s3.bucket-name=${AWS_S3_BUCKET_NAME:pmum-qr-death-case}
aws.s3.region=${AWS_S3_REGION:ap-south-1}
aws.s3.access-key=${AWS_S3_ACCESS_KEY:}
aws.s3.secret-key=${AWS_S3_SECRET_KEY:}
# Insurance email:
insurance.export.recipient=Sbilife.org.in@gmail.com
reports.live.pending-profile-key=PMUMS_PENDING_PROFILE_LIVE_REPORT_2026_SECURE_KEY
